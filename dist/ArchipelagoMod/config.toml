# Mod options
enabled = true
reload = "F7" # Recommended to use from the song list.
deathlink_percent = "100" # 0-100, default 100. Percentage of total HP to lose on receive.
trap_duration = "15.0" # 0-180, default 15.0 Seconds until traps expire. 0 to not expire for current attempt.
icon_reroll = "60.0" # 0.0, default 60.0 Milliseconds between icon rerolls while Icon trap is active. 0 to not reroll.

# Mod information
name = "Archipelago"
author = "Cynichill"
version = "1.1.1"
dll = ["ArchipelagoMod.dll"]
include = ["AP", "."]
